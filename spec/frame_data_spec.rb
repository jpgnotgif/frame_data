require 'spec_helper'

describe FrameData do
  it 'has a version number' do
    expect(FrameData::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
